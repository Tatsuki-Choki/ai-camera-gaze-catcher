# 使い方ガイド

## 基本操作

1. アプリを開き、動画ファイル（MP4 / MOV / WebM）を選択します。
2. 解析の細かさを選びます。
   - 高速: 約1秒ごとに判定します。
   - 標準: 約0.5秒ごとに判定します。
   - 高精度: 約0.25秒ごとに判定します。
   - どのモードでも、高スコアの瞬間の付近は自動でさらに細かく解析します。
3. 検出感度を調整します。候補を広く拾うなら甘め、正面に近いものだけに絞るなら厳しめにします。
4. 保存形式（JPEG / PNG）を選びます。
5. 「解析を開始」を押します。解析中も動画プレイヤーは自由に操作できます。
6. 動画の下に視線スコアのタイムラインが表示されます。クリック（または左右キー）でその時刻へジャンプできます。
7. 候補カードのサムネイルをクリックすると、動画がその時刻へ移動します。
8. 候補一覧で必要な候補を選択し、個別保存・コピー・ZIP保存します。時刻順 / スコア順で並べ替えできます。

## 解析エンジンについて

- MP4 / MOV形式では高速エンジン（WebCodecs）を使い、バックグラウンドで解析します。
- WebM形式や非対応ブラウザでは自動的に互換モードへ切り替わります（解析に時間がかかります）。
- まばたき中・ブレているフレームは自動的に避け、各ピークから最良の1枚を選びます。

## うまく使うコツ

- 長い動画はまず「高速」または「標準」で候補を拾ってください。
- 候補が多すぎる場合は感度を上げて再解析してください。
- 候補が少なすぎる場合は感度を下げるか、「高精度」で再解析してください。
- タイムラインの山（ピーク）を見ると、どの辺りにカメラ目線が多いか一目で分かります。

## プライバシー

動画解析はブラウザ内で行います。動画ファイルそのものは外部サーバーへ送信されません。ただし、AIモデルとWASMの初回読み込みにはCDNへの通信が発生します。
